import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("branchDAM Mobile Initialized")
                .font(.headline)
        }
        .padding()
    }
}
